[auth]
redirect_uri = "http://localhost:8501/oauth2callback"
cookie_secret = "replace_with_a_long_random_secret"

[database]
# Required: set PostgreSQL URL for the app backend.
# url = "postgresql://pystreamline_app:your_password@localhost:5432/pystreamline"

[smtp]
# Required for email OTP verification
host = "smtp.yourmail.com"
port = 587
username = "replace_with_smtp_username"
password = "replace_with_smtp_password"
from_email = "no-reply@yourdomain.com"
use_tls = true

[email_otp]
# Optional: keep this secret and random.
# If omitted, app falls back to [auth].cookie_secret.
pepper = "replace_with_long_random_secret"
# Optional overrides
ttl_minutes = 10
resend_seconds = 60
max_attempts = 5
signup_request_ttl_hours = 24

[auth.google]
client_id = "replace_with_google_client_id"
client_secret = "replace_with_google_client_secret"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

[auth.linkedin]
client_id = "replace_with_linkedin_client_id"
client_secret = "replace_with_linkedin_client_secret"
server_metadata_url = "https://www.linkedin.com/oauth/.well-known/openid-configuration"
client_kwargs = { scope = "openid profile email" }

[promo]
# Optional. Default behavior is disabled unless enabled = true.
enabled = false
valid_codes = ["WELCOME2026", "EARLYACCESS"]
